{
  "name": "htmlody-",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "seed": "bun ./src/utils/seed.ts",
    "serve": "bun ./index.ts",
    "dev": "bun --hot ./index.ts",
    "stripe-webhook": "stripe listen --forward-to localhost:3000/stripe-webhook"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "bnkit": "^0.3.8",
    "stripe": "^14.2.0"
  }
}
