{
  "name": "bnk-payments-stack",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "seed-user-stripe": "bun ./src/db/seeds/seed-user-stripe.ts",
    "seed-mac-common": "bun ./src/db/seeds/seed-mac-common.ts",
    "serve": "bun ./index.ts",
    "dev": "bun --hot ./index.ts",
    "stripe-webhook": "stripe listen --forward-to localhost:3000/stripe-webhook"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "bnkit": "^0.3.8",
    "bun": "^1.0.12",
    "stripe": "^14.2.0"
  }
}
